import { Button } from "react-native"

export const ForgotPasswordFunc = () => {
    return(
        <View>
            <Button
                title="Forgot Password"
            />
        </View>
    )
}